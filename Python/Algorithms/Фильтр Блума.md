#algorithm 

Bloom filters are similar to the `Set` data structure. You can add items to them, and check if an item is present.
Bloom filters are probabilistic data structure. Bloom filters can give definite "no"s, but they can't be certain about "yes."
A bloom filter will _never_ give a false-negative.
Picking the correct number of hash functions and bits for a bloom filter is a fine balance.